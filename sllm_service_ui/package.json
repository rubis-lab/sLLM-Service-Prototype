{
  "name": "local-llm-chat",
  "version": "1.0.0",
  "private": true,
  "description": "SLLM service UI with local model, KG context, and report export support.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
